[nosetests]
# enable coverage
with-coverage=1

# cover only the main package tap_postgres
cover-package=tap_postgres

# set coverage minimum to 50, it's the current value
cover-min-percentage=50

# product html coverage report
cover-html=1

# folder where to produce the html coverage report
cover-html-dir=coverage_report